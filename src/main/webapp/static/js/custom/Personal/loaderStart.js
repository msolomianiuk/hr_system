$(window).on('load', function () {
    $('.loading').attr('style', 'display: flex');
    $('.loading .questions_text').addClass('hidden');
    $('.loading .candidateList_text').removeClass('hidden');
});